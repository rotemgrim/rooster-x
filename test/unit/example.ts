import { expect } from 'chai';
import { describe, it } from "mocha";

describe("Example Test", () => {
    it('checks that typescript testing is working', () => {
        expect(null).to.equal(null);
    });
});
